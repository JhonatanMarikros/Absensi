import 'package:flutter/material.dart';

class PanduanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Panduan Page"),
    );
  }
}
